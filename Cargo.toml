[workspace]
members = [
    "abi-gen",
    "abi-codec",
    "prelude",
    "primitives",
    "alloc",
    "core",
    "lang",
]
exclude = [
    "examples",
]

[profile.release]
panic = "abort"
lto = true
